#include "match.h"
#include "benchmark.h"
#include "postMatch.h"

int main() { 
    // play(1000 * 60 * 1, 100, "Ascendant", "Aegis");
    //testUpTo("alphabeta", "climb mo v5", 8, 8);
    int id = findLowestUnusedID();
    std::cout << id;
 }
