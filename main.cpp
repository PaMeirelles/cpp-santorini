#include "match.h"
#include "benchmark.h"
#include "postMatch.h"
#include "hashTable.h"

int main() { 
    play(1000 * 60 * 30, 1, "Ascendant", "Cosmic");
    // Board b = Board();
    // auto oi = getBestMove(b, alphabeta, nh_s, et_s, 1000 * 15 * 60 * 5);
    // std::cout << std::endl;
    // auto oioi = getBestMove(b, mvb3, nh_s, et_s, 1000 * 15 * 60 * 5);

 }
